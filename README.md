Overview
This branch contains the foundational code for the Student Record Management System. It defines the core data structure, initializes memory, handles the user greeting, and ensures clean memory release on program exit.
